import { List, styled } from "@mui/material";

export const CustomizeMainPageList = styled(List)(() => ({
  display: "flex",
  flexWrap: "wrap",
  justifyContent: "space-between",
  alignItems: "revent",
}));
